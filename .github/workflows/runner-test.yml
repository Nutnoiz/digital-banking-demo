name: Self-hosted Runner Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    name: Test Self-hosted Runner
    runs-on: [self-hosted, docker-desktop-test]

    steps:
      - name: Test Runner
        shell: powershell
        run: |
          Write-Host "Self-hosted runner test started"
          Write-Host "Runner: $env:RUNNER_NAME"
          Write-Host "OS: $env:RUNNER_OS"

          $kubectl = "C:\Users\Admin\AppData\Local\Programs\DockerDesktop\resources\bin\kubectl.exe"

          Write-Host "kubectl path: $kubectl"
          Write-Host "kubectl exists: $(Test-Path $kubectl)"

          & $kubectl version --client

          & $kubectl config current-context
          & $kubectl get nodes

